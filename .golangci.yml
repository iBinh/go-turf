linters:
  enable:
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
  disable:
    - errcheck
    - stylecheck
run:
  timeout: 5m
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - ineffassign
