linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
  disable:
    - stylecheck
run:
  timeout: 5m
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - ineffassign
